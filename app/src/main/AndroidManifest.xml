<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beyondzero.loise.marathon"
    android:versionCode="7"
    android:versionName="1.6">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->

    <uses-permission android:name="android.permission.READ_PROFILE" />

    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".Adapter.VolleyApplication"
        android:allowBackup="true"
        android:icon="@drawable/fl"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".About"
            android:theme="@style/MyActionBar" />
        <activity android:name=".Facebook" />
        <activity android:name=".Problem" />
        <activity
            android:name=".Listen"
            android:theme="@style/MyActionBar" />
        <activity android:name=".twitter" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="c4187f8ac4cb5d8068a1328986d55d80386316d1" />

        <activity
            android:name=".Calender"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".Statistics"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".News"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".Donate"
            android:label="Donate"
            android:theme="@style/MyActionBar" />
        <activity android:name=".Instagram" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <activity android:name=".Webview" />
        <activity
            android:name=".RegistrationInfo"
            android:theme="@style/MyActionBar" />
        <activity android:name=".TransactionId" />
        <activity
            android:name=".Register2"
            android:label="@string/title_activity_register2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationInfo"
            android:theme="@style/MyActionBar" />
        <activity android:name=".FAQsActivity" />
        <activity
            android:name=".RouteList"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".NewsInfo"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".FittnessInfo"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".Fitnesss"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".MainActivityRss"
            android:theme="@style/MyActionBar" />

        <service android:name=".RssService" />

        <activity android:name=".Youtube" />


        <!--E4:66:05:64:91:AE:7B:70:EE:2F:E6:09:8E:93:76:50:5D:04:34:AC-->

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBMosLxBNM6C2iqdMdasrBdI4_x5q7_PgY" />

        <activity
            android:name=".Maptwentyone"
            android:label="@string/title_activity_maptwentyone" />
        <activity
            android:name=".Fivekms"
            android:label="@string/title_activity_fivekms" />
        <activity
            android:name=".Tenkms"
            android:label="@string/title_activity_tenkms" />
        <activity
            android:name=".Twokms"
            android:label="@string/title_activity_twokms"></activity>
    </application>

</manifest>
